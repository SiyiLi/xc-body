diff --git a/firmware/main/boards/stackchan/avatar_set.cc b/firmware/main/boards/stackchan/avatar_set.cc
index cc95730..79c9c4c 100644
--- a/firmware/main/boards/stackchan/avatar_set.cc
+++ b/firmware/main/boards/stackchan/avatar_set.cc
@@ -14,7 +14,8 @@ AvatarSet::~AvatarSet() {
 }

 const lv_image_dsc_t* AvatarSet::GetFace(int face_index) const {
-    if (!loaded_ || mode_ != Mode::kLayered) {
+    if (!loaded_ ||
+        (mode_ != Mode::kLayered && mode_ != Mode::kLayeredNative)) {
         return nullptr;
     }
     if (face_index < 0 || face_index >= kNumFaces) {
@@ -24,7 +25,8 @@ const lv_image_dsc_t* AvatarSet::GetFace(int face_index) const {
 }

 const lv_image_dsc_t* AvatarSet::GetEyes(int eyes_index) const {
-    if (!loaded_ || mode_ != Mode::kLayered) {
+    if (!loaded_ ||
+        (mode_ != Mode::kLayered && mode_ != Mode::kLayeredNative)) {
         return nullptr;
     }
     if (eyes_index < 0 || eyes_index >= kNumEyes) {
@@ -34,7 +36,8 @@ const lv_image_dsc_t* AvatarSet::GetEyes(int eyes_index) const {
 }

 const lv_image_dsc_t* AvatarSet::GetMouth(int mouth_index) const {
-    if (!loaded_ || mode_ != Mode::kLayered) {
+    if (!loaded_ ||
+        (mode_ != Mode::kLayered && mode_ != Mode::kLayeredNative)) {
         return nullptr;
     }
     if (mouth_index < 0 || mouth_index >= kNumMouths) {
@@ -64,8 +67,12 @@ bool AvatarSet::AdoptOwnedBuffer(Mode mode, uint8_t* owned_buffer, size_t image_
         return false;
     }

-    const size_t expected =
-        (mode == Mode::kLayered) ? kLayeredPayloadBytes : kMatrixPayloadBytes;
+    size_t expected = kMatrixPayloadBytes;
+    if (mode == Mode::kLayered) {
+        expected = kLayeredPayloadBytes;
+    } else if (mode == Mode::kLayeredNative) {
+        expected = kLayeredNativePayloadBytes;
+    }
     if (image_data_size != expected) {
         ESP_LOGW(TAG,
                  "AdoptOwnedBuffer: size mismatch (got %u, expected %u for mode=%d)",
@@ -86,26 +93,31 @@ bool AvatarSet::AdoptOwnedBuffer(Mode mode, uint8_t* owned_buffer, size_t image_
     image_buffer_size_ = image_data_size;
     mode_ = mode;

-    if (mode == Mode::kLayered) {
+    if (mode == Mode::kLayered || mode == Mode::kLayeredNative) {
+        const bool native = mode == Mode::kLayeredNative;
+        const int width = native ? kNativeImageWidth : kImageWidth;
+        const int height = native ? kNativeImageHeight : kImageHeight;
+        const size_t image_bytes = native ? kNativeImageBytes : kImageBytes;
         size_t offset = 0;
         for (int i = 0; i < kNumFaces; ++i) {
-            InitImageHeader(&face_table_[i]);
+            InitImageHeader(&face_table_[i], width, height, image_bytes);
             face_table_[i].data = image_buffer_ + offset;
-            offset += kImageBytes;
+            offset += image_bytes;
         }
         for (int i = 0; i < kNumEyes; ++i) {
-            InitImageHeader(&eyes_table_[i]);
+            InitImageHeader(&eyes_table_[i], width, height, image_bytes);
             eyes_table_[i].data = image_buffer_ + offset;
-            offset += kImageBytes;
+            offset += image_bytes;
         }
         for (int i = 0; i < kNumMouths; ++i) {
-            InitImageHeader(&mouth_table_[i]);
+            InitImageHeader(&mouth_table_[i], width, height, image_bytes);
             mouth_table_[i].data = image_buffer_ + offset;
-            offset += kImageBytes;
+            offset += image_bytes;
         }
     } else {
         for (int i = 0; i < kMatrixSize; ++i) {
-            InitImageHeader(&matrix_table_[i]);
+            InitImageHeader(
+                &matrix_table_[i], kImageWidth, kImageHeight, kImageBytes);
             matrix_table_[i].data = image_buffer_ + static_cast<size_t>(i) * kImageBytes;
         }
     }
@@ -133,14 +145,15 @@ void AvatarSet::Unload() {
     loaded_ = false;
 }

-void AvatarSet::InitImageHeader(lv_image_dsc_t* dsc) {
+void AvatarSet::InitImageHeader(
+    lv_image_dsc_t* dsc, int width, int height, size_t image_bytes) {
     dsc->header.magic     = LV_IMAGE_HEADER_MAGIC;
     dsc->header.cf        = LV_COLOR_FORMAT_RGB565;
     dsc->header.flags     = 0;
-    dsc->header.w         = kImageWidth;
-    dsc->header.h         = kImageHeight;
-    dsc->header.stride    = kImageWidth * 2;
+    dsc->header.w         = width;
+    dsc->header.h         = height;
+    dsc->header.stride    = width * 2;
     dsc->header.reserved_2 = 0;
-    dsc->data_size = kImageBytes;
+    dsc->data_size = image_bytes;
     // `data` is set by the caller (PSRAM offset).
 }
diff --git a/firmware/main/boards/stackchan/avatar_set.h b/firmware/main/boards/stackchan/avatar_set.h
index 9d2e53f..2a59348 100644
--- a/firmware/main/boards/stackchan/avatar_set.h
+++ b/firmware/main/boards/stackchan/avatar_set.h
@@ -24,8 +24,9 @@
 class AvatarSet {
 public:
     enum class Mode : uint8_t {
-        kLayered = 0,  // 14 symbols (face 6 + eyes 3 + mouth 5)
-        kMatrix  = 1,  // 90 symbols (face 6 × eyes 3 × mouth 5)
+        kLayered = 0,        // 14 symbols at 160x120
+        kMatrix  = 1,         // 90 symbols at 160x120
+        kLayeredNative = 2,  // 14 symbols at native 320x240
     };

     static constexpr int kNumFaces  = 6;  // idle / happy / thinking / sad / surprised / embarrassed
@@ -33,18 +34,24 @@ public:
     static constexpr int kNumMouths = 5;  // closed / half / open / e / u
     static constexpr int kMatrixSize = kNumFaces * kNumEyes * kNumMouths;  // 90

-    // Fixed geometry — matches firmware/scripts/avatar_convert/convert_avatars.py
-    // (TARGET_W / TARGET_H) and the LVGL scale already applied in stackchan.cc.
-    static constexpr int kImageWidth  = 160;
+    // Legacy geometry remains available for existing runtime/static sets.
+    static constexpr int kImageWidth = 160;
     static constexpr int kImageHeight = 120;
+    static constexpr int kNativeImageWidth = 320;
+    static constexpr int kNativeImageHeight = 240;
     static constexpr size_t kImageBytes =
-        static_cast<size_t>(kImageWidth) * static_cast<size_t>(kImageHeight) * 2;  // RGB565 LE
+        static_cast<size_t>(kImageWidth) * kImageHeight * 2;
+    static constexpr size_t kNativeImageBytes =
+        static_cast<size_t>(kNativeImageWidth) * kNativeImageHeight * 2;

     // Expected raw payload sizes (for early size checks at the loader boundary).
     static constexpr size_t kLayeredPayloadBytes =
         static_cast<size_t>(kNumFaces + kNumEyes + kNumMouths) * kImageBytes;   // 14 * 38400
     static constexpr size_t kMatrixPayloadBytes =
-        static_cast<size_t>(kMatrixSize) * kImageBytes;                          // 90 * 38400
+        static_cast<size_t>(kMatrixSize) * kImageBytes;
+    static constexpr size_t kLayeredNativePayloadBytes =
+        static_cast<size_t>(kNumFaces + kNumEyes + kNumMouths) *
+        kNativeImageBytes;

     AvatarSet();
     ~AvatarSet();
@@ -114,5 +121,6 @@ private:

     // Fill the fixed fields (magic / cf / w / h / stride / data_size) of a
     // descriptor. The caller still sets `data` to the PSRAM offset.
-    static void InitImageHeader(lv_image_dsc_t* dsc);
+    static void InitImageHeader(
+        lv_image_dsc_t* dsc, int width, int height, size_t image_bytes);
 };
diff --git a/firmware/main/boards/stackchan/stackchan.cc b/firmware/main/boards/stackchan/stackchan.cc
index 41630fd..83f77b4 100644
--- a/firmware/main/boards/stackchan/stackchan.cc
+++ b/firmware/main/boards/stackchan/stackchan.cc
@@ -4271,7 +4271,8 @@ private:

     const lv_image_dsc_t* FaceImageForIndex(int face_index) const {
         if (avatar_set_.is_loaded() &&
-            avatar_set_.mode() == AvatarSet::Mode::kLayered) {
+            (avatar_set_.mode() == AvatarSet::Mode::kLayered ||
+             avatar_set_.mode() == AvatarSet::Mode::kLayeredNative)) {
             const lv_image_dsc_t* dsc = avatar_set_.GetFace(face_index);
             if (dsc != nullptr) return dsc;
         }
@@ -4288,7 +4289,8 @@ private:

     const lv_image_dsc_t* EyesImageForIndex(int eyes_index) const {
         if (avatar_set_.is_loaded() &&
-            avatar_set_.mode() == AvatarSet::Mode::kLayered) {
+            (avatar_set_.mode() == AvatarSet::Mode::kLayered ||
+             avatar_set_.mode() == AvatarSet::Mode::kLayeredNative)) {
             const lv_image_dsc_t* dsc = avatar_set_.GetEyes(eyes_index);
             if (dsc != nullptr) return dsc;
         }
@@ -4302,7 +4304,8 @@ private:

     const lv_image_dsc_t* MouthImageForIndex(int mouth_index) const {
         if (avatar_set_.is_loaded() &&
-            avatar_set_.mode() == AvatarSet::Mode::kLayered) {
+            (avatar_set_.mode() == AvatarSet::Mode::kLayered ||
+             avatar_set_.mode() == AvatarSet::Mode::kLayeredNative)) {
             const lv_image_dsc_t* dsc = avatar_set_.GetMouth(mouth_index);
             if (dsc != nullptr) return dsc;
         }
@@ -4349,6 +4352,9 @@ private:
         if (dsc == nullptr) return false;
         if (!EnsureAvatarObject()) return false;
         lv_image_set_src(avatar_img_, dsc);
+        const int scale = dsc->header.w == 320 && dsc->header.h == 240
+            ? 256 : 512;
+        lv_image_set_scale(avatar_img_, scale);
         lv_obj_move_foreground(avatar_img_);
         BringListeningIndicatorToFrontLocked();
         return true;
@@ -4478,8 +4484,8 @@ private:
         if (avatar_img_ == nullptr) {
             return false;
         }
-        // Center on the 320x240 LCD and upscale 160x120 -> ~320x240 (2x).
-        // lv_image_set_scale uses 256 = 1.0x; 512 = 2.0x.
+        // RenderAvatarLocked selects 1x for native 320x240 descriptors and
+        // 2x for legacy/static 160x120 descriptors.
         lv_image_set_scale(avatar_img_, 512);
         lv_obj_align(avatar_img_, LV_ALIGN_CENTER, 0, 0);
         lv_obj_clear_flag(avatar_img_, LV_OBJ_FLAG_SCROLLABLE);
@@ -7183,6 +7189,8 @@ public:
             mode_enum = AvatarSet::Mode::kLayered;
         } else if (strcmp(mode_j->valuestring, "matrix") == 0) {
             mode_enum = AvatarSet::Mode::kMatrix;
+        } else if (strcmp(mode_j->valuestring, "layered-320x240") == 0) {
+            mode_enum = AvatarSet::Mode::kLayeredNative;
         } else {
             ESP_LOGW(TAG, "OnAvatarSetFetch: unknown mode '%s'", mode_j->valuestring);
             SendAvatarSetLoadedError(req_checksum, "unknown_mode");
diff --git a/gateway/stackchan_mcp/capture_server.py b/gateway/stackchan_mcp/capture_server.py
index a235f17..4c7ae1a 100644
--- a/gateway/stackchan_mcp/capture_server.py
+++ b/gateway/stackchan_mcp/capture_server.py
@@ -95,11 +95,30 @@ class _AvatarStaging:
 # against a misbehaving / malicious uploader without inviting unbounded
 # disk consumption on the gateway host.
 CAPTURE_MAX_BYTES = 8 * 1024 * 1024
+PREPARED_OPUS_MAX_TOTAL_BYTES = 768 * 1024
+PREPARED_OPUS_MAX_PACKET_BYTES = 1275
+PREPARED_OPUS_MAX_PACKETS = 4096
+PREPARED_OPUS_MAX_RECORDED_IDS = 1024
+PREPARED_OPUS_FRAME_DURATION_MS = 60
+PREPARED_OPUS_PREFILL_PACKETS = 6
+PREPARED_OPUS_START_DELAY_S = 0.05
+PREPARED_OPUS_RESULTS_KEY = web.AppKey("prepared_opus_results", dict)
+PREPARED_OPUS_LOCK_KEY = web.AppKey("prepared_opus_lock", asyncio.Lock)
+_MESSAGE_ID_FIRST_CHARS = frozenset(
+    "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
+)
+_MESSAGE_ID_CHARS = _MESSAGE_ID_FIRST_CHARS | frozenset("._:-")
+


 def _is_authorized(auth_header: str, expected_token: str) -> bool:
     """Return whether the bearer auth header matches the expected token."""
-    return auth_header == f"Bearer {expected_token}"
+    if not expected_token:
+        return False
+    return secrets.compare_digest(
+        auth_header,
+        f"Bearer {expected_token}",
+    )


 async def handle_capture(request: web.Request) -> web.Response:
@@ -216,7 +234,7 @@ async def stage_avatar_set(
     The staging entry is consumed on the first successful fetch and
     GC'd after AVATAR_SET_STAGING_TTL_SEC if never fetched.
     """
-    if mode not in ("layered", "matrix"):
+    if mode not in ("layered", "matrix", "layered-320x240"):
         raise ValueError(f"unknown avatar mode: {mode}")

     short_id = secrets.token_hex(8)
@@ -432,6 +451,261 @@ async def handle_pcm(request: web.Request) -> web.Response:
     return web.Response(text=json.dumps(result), content_type="application/json")


+class PreparedOpusValidationError(ValueError):
+    """A prepared Opus body does not match the framed wire contract."""
+
+
+class PreparedOpusTooLargeError(PreparedOpusValidationError):
+    """A prepared Opus body exceeds its bounded upload size."""
+
+
+def _validate_prepared_opus_message_id(message_id: str) -> None:
+    """Require the pending-thought recent-ID key contract."""
+
+    if (
+        not 1 <= len(message_id) <= 128
+        or message_id[0] not in _MESSAGE_ID_FIRST_CHARS
+        or any(character not in _MESSAGE_ID_CHARS for character in message_id)
+    ):
+        raise PreparedOpusValidationError(
+            "X-Message-Id has an invalid format"
+        )
+
+
+def _validate_prepared_opus_packet(packet: bytes) -> None:
+    """Check the mono flag and duration exposed by an Opus packet TOC.
+
+    The producer guarantees raw Opus encoded from 16 kHz mono PCM. The raw
+    packet does not expose that PCM input rate, and this check does not claim
+    that an Opus decoder will accept arbitrary bytes with a matching TOC.
+    """
+
+    if packet[0] & 0x04:
+        raise PreparedOpusValidationError(
+            "prepared Opus packets must declare mono"
+        )
+    duration_ms = _opus_packet_duration_ms(packet)
+    if duration_ms != PREPARED_OPUS_FRAME_DURATION_MS:
+        raise PreparedOpusValidationError(
+            "prepared Opus packets must contain exactly 60 ms "
+            f"(got {duration_ms:g} ms)"
+        )
+
+
+def _opus_packet_duration_ms(packet: bytes) -> float:
+    """Return duration from the Opus TOC without claiming decodability."""
+
+    toc = packet[0]
+    config = toc >> 3
+    if config < 12:
+        frame_duration_ms = (10, 20, 40, 60)[config & 0x03]
+    elif config < 16:
+        frame_duration_ms = (10, 20)[config & 0x01]
+    else:
+        frame_duration_ms = (2.5, 5, 10, 20)[config & 0x03]
+
+    frame_code = toc & 0x03
+    if frame_code == 0:
+        frame_count = 1
+    elif frame_code in (1, 2):
+        frame_count = 2
+    else:
+        if len(packet) < 2:
+            raise PreparedOpusValidationError(
+                "prepared Opus packet omits its frame count"
+            )
+        frame_count = packet[1] & 0x3F
+        if frame_count == 0:
+            raise PreparedOpusValidationError(
+                "prepared Opus packet has zero frames"
+            )
+    return frame_duration_ms * frame_count
+
+
+def _parse_prepared_opus(payload: bytes) -> list[bytes]:
+    """Parse framing and validate byte-verifiable Opus profile facts."""
+
+    if not payload:
+        raise PreparedOpusValidationError("prepared Opus body is empty")
+    if len(payload) > PREPARED_OPUS_MAX_TOTAL_BYTES:
+        raise PreparedOpusTooLargeError(
+            "prepared Opus body exceeds "
+            f"{PREPARED_OPUS_MAX_TOTAL_BYTES} bytes"
+        )
+
+    packets: list[bytes] = []
+    offset = 0
+    while offset < len(payload):
+        if len(packets) >= PREPARED_OPUS_MAX_PACKETS:
+            raise PreparedOpusValidationError(
+                "prepared Opus body exceeds "
+                f"{PREPARED_OPUS_MAX_PACKETS} packets"
+            )
+        remaining = len(payload) - offset
+        if remaining < 2:
+            raise PreparedOpusValidationError(
+                "prepared Opus body has trailing bytes after final packet"
+            )
+        packet_size = int.from_bytes(payload[offset : offset + 2], "big")
+        offset += 2
+        if packet_size == 0:
+            raise PreparedOpusValidationError(
+                "prepared Opus packet must not be empty"
+            )
+        if packet_size > PREPARED_OPUS_MAX_PACKET_BYTES:
+            raise PreparedOpusValidationError(
+                "prepared Opus packet exceeds "
+                f"{PREPARED_OPUS_MAX_PACKET_BYTES} bytes"
+            )
+        packet_end = offset + packet_size
+        if packet_end > len(payload):
+            raise PreparedOpusValidationError(
+                "prepared Opus body ends inside a packet"
+            )
+        packet = payload[offset:packet_end]
+        _validate_prepared_opus_packet(packet)
+        packets.append(packet)
+        offset = packet_end
+
+    if offset != len(payload):
+        raise PreparedOpusValidationError(
+            "prepared Opus body has trailing data"
+        )
+    return packets
+
+
+async def _read_prepared_opus_body(request: web.Request) -> bytes:
+    """Read a bounded request body even when Content-Length is absent."""
+
+    content_length = request.content_length
+    if (
+        content_length is not None
+        and content_length > PREPARED_OPUS_MAX_TOTAL_BYTES
+    ):
+        raise PreparedOpusTooLargeError(
+            "prepared Opus body exceeds "
+            f"{PREPARED_OPUS_MAX_TOTAL_BYTES} bytes"
+        )
+
+    payload = bytearray()
+    async for chunk in request.content.iter_chunked(8192):
+        payload.extend(chunk)
+        if len(payload) > PREPARED_OPUS_MAX_TOTAL_BYTES:
+            raise PreparedOpusTooLargeError(
+                "prepared Opus body exceeds "
+                f"{PREPARED_OPUS_MAX_TOTAL_BYTES} bytes"
+            )
+    return bytes(payload)
+
+
+async def _play_prepared_opus(gateway: "Gateway", packets: list[bytes]) -> None:
+    """Play one validated packet sequence on the shared device audio path."""
+
+    esp32 = gateway.esp32
+    tts_lock = getattr(esp32, "tts_lock", None)
+    if tts_lock is None:
+        raise RuntimeError("Gateway does not expose the TTS playback lock")
+
+    async with tts_lock:
+        started = False
+        try:
+            await esp32.send_tts_state("start")
+            started = True
+            await asyncio.sleep(PREPARED_OPUS_START_DELAY_S)
+            loop = asyncio.get_running_loop()
+            prefill_count = min(
+                len(packets), PREPARED_OPUS_PREFILL_PACKETS
+            )
+            for packet in packets[:prefill_count]:
+                await esp32.send_audio_frame(packet)
+            next_send_time = loop.time() + (
+                PREPARED_OPUS_FRAME_DURATION_MS / 1000
+            )
+            for packet in packets[prefill_count:]:
+                delay = next_send_time - loop.time()
+                if delay > 0:
+                    await asyncio.sleep(delay)
+                await esp32.send_audio_frame(packet)
+                next_send_time = max(next_send_time, loop.time())
+                next_send_time += PREPARED_OPUS_FRAME_DURATION_MS / 1000
+        finally:
+            if started:
+                try:
+                    await esp32.send_tts_state("stop")
+                except ConnectionError:
+                    pass
+
+
+async def handle_prepared_opus(request: web.Request) -> web.Response:
+    """Play encoded Opus with bounded process-local duplicate suppression."""
+
+    expected_token = request.app[PCM_TOKEN_KEY]
+    if not _is_authorized(
+        request.headers.get("Authorization", ""), expected_token
+    ):
+        logger.warning("Prepared Opus upload auth rejected")
+        return web.json_response({"error": "Unauthorized"}, status=401)
+    if request.content_type != "application/octet-stream":
+        return web.json_response(
+            {"error": "Content-Type must be application/octet-stream"},
+            status=415,
+        )
+
+    message_id = request.headers.get("X-Message-Id", "")
+    try:
+        _validate_prepared_opus_message_id(message_id)
+    except PreparedOpusValidationError as exc:
+        return web.json_response({"error": str(exc)}, status=400)
+
+    gateway = request.app[GATEWAY_KEY]
+    if gateway is None:
+        return web.json_response({"error": "Gateway not available"}, status=503)
+
+    try:
+        payload = await _read_prepared_opus_body(request)
+        packets = _parse_prepared_opus(payload)
+    except PreparedOpusTooLargeError as exc:
+        return web.json_response({"error": str(exc)}, status=413)
+    except PreparedOpusValidationError as exc:
+        return web.json_response({"error": str(exc)}, status=400)
+
+    results = request.app[PREPARED_OPUS_RESULTS_KEY]
+    async with request.app[PREPARED_OPUS_LOCK_KEY]:
+        cached = results.get(message_id)
+        if cached is not None:
+            logger.info(
+                "Prepared Opus duplicate suppressed: message_id=%s",
+                message_id,
+            )
+            return web.json_response(cached)
+        try:
+            await _play_prepared_opus(gateway, packets)
+        except ConnectionError as exc:
+            return web.json_response(
+                {"error": f"Device unavailable: {exc}"},
+                status=503,
+            )
+        except RuntimeError as exc:
+            return web.json_response({"error": str(exc)}, status=500)
+
+        result = {
+            "ok": True,
+            "packet_count": len(packets),
+            "duration_ms": len(packets) * PREPARED_OPUS_FRAME_DURATION_MS,
+        }
+        results[message_id] = result
+        while len(results) > PREPARED_OPUS_MAX_RECORDED_IDS:
+            results.pop(next(iter(results)))
+
+    logger.info(
+        "Prepared Opus played: message_id=%s packets=%d bytes=%d",
+        message_id,
+        len(packets),
+        len(payload),
+    )
+    return web.json_response(result)
+
+
 def create_capture_app(
     capture_token: str = "",
     pcm_token: str = "",
@@ -478,4 +744,7 @@ def create_capture_app(
     app.router.add_post("/capture", handle_capture)
     app.router.add_get("/avatar_set/{short_id}", handle_avatar_set_fetch)
     app.router.add_post("/pcm", handle_pcm)
+    app[PREPARED_OPUS_RESULTS_KEY] = {}
+    app[PREPARED_OPUS_LOCK_KEY] = asyncio.Lock()
+    app.router.add_post("/opus", handle_prepared_opus)
     return app
diff --git a/gateway/stackchan_mcp/gateway.py b/gateway/stackchan_mcp/gateway.py
index 68bd753..6ce38df 100644
--- a/gateway/stackchan_mcp/gateway.py
+++ b/gateway/stackchan_mcp/gateway.py
@@ -256,10 +256,12 @@ class Gateway:
         with open(archive_path, "rb") as f:
             payload = f.read()

-        kimg_bytes = 160 * 120 * 2  # 38_400 — matches AvatarSet::kImageBytes
+        legacy_image_bytes = 160 * 120 * 2
+        native_image_bytes = 320 * 240 * 2
         expected = {
-            "layered": 14 * kimg_bytes,   # 537_600
-            "matrix":  90 * kimg_bytes,   # 3_456_000
+            "layered": 14 * legacy_image_bytes,
+            "matrix": 90 * legacy_image_bytes,
+            "layered-320x240": 14 * native_image_bytes,
         }.get(mode)
         if expected is None:
             return {"ok": False, "error": f"unknown_mode: {mode}"}
diff --git a/gateway/stackchan_mcp/esp32_client.py b/gateway/stackchan_mcp/esp32_client.py
--- a/gateway/stackchan_mcp/esp32_client.py
+++ b/gateway/stackchan_mcp/esp32_client.py
@@ -136,6 +136,7 @@ class ESP32Connection:
         self._initialized = False
         self._tools_discovered = False
         self._avatar_render_sent = False
+        self._active_avatar_checksum = ""
         # Phase 4.5 avatar: pending load_avatar_set calls waiting for the
         # device's `avatar_set_loaded` reply. Keyed by expected checksum
         # so that overlapping fetches (different sets) can be discriminated.
@@ -280,7 +281,18 @@ class ESP32Connection:
+        if self._active_avatar_checksum == checksum:
+            return {
+                "ok": True,
+                "checksum": checksum,
+                "already_loaded": True,
+            }
+        previous = self._avatar_set_waiters.get(checksum)
+        if previous is not None and not previous.done():
+            try:
+                return await asyncio.wait_for(
+                    asyncio.shield(previous), timeout=timeout
+                )
+            except asyncio.TimeoutError:
+                return {"ok": False, "checksum": checksum, "error": "device_timeout"}
+            except asyncio.CancelledError:
+                return {"ok": False, "checksum": checksum, "error": "superseded"}
         future: asyncio.Future[dict[str, Any]] = asyncio.get_event_loop().create_future()
-        # Last-writer-wins on duplicate checksum: cancel the previous waiter
-        # so the same set being re-pushed doesn't strand callers.
-        previous = self._avatar_set_waiters.pop(checksum, None)
-        if previous is not None and not previous.done():
-            previous.cancel()
         self._avatar_set_waiters[checksum] = future
@@ -312,6 +324,8 @@ class ESP32Connection:
     def handle_avatar_set_loaded(self, payload: dict[str, Any]) -> None:
         """Resolve a pending send_avatar_set_fetch by checksum."""
         checksum = payload.get("checksum", "")
+        if payload.get("ok") is True:
+            self._active_avatar_checksum = checksum
         future = self._avatar_set_waiters.pop(checksum, None)
         if future is not None and not future.done():
             future.set_result(payload)
diff --git a/gateway/stackchan_mcp/stdio_server.py b/gateway/stackchan_mcp/stdio_server.py
index 91a4131..77b8840 100644
--- a/gateway/stackchan_mcp/stdio_server.py
+++ b/gateway/stackchan_mcp/stdio_server.py
@@ -886,7 +886,7 @@ async def _dispatch_mcp_tool(
                     ),
                 )
             ]
-        if mode not in ("layered", "matrix"):
+        if mode not in ("layered", "matrix", "layered-320x240"):
             return [
                 TextContent(
                     type="text",
@@ -2772,7 +2772,9 @@ def create_server(notify_config: NotifyConfig | None = None) -> StackChanServer:
                     "raw RGB565 file on the gateway host: layered mode = "
                     "14 frames (face 6 + eyes 3 + mouth 5) totalling "
                     "537,600 bytes; matrix mode = 90 frames (6 × 3 × 5) "
-                    "totalling 3,456,000 bytes. Returns ok / checksum / "
+                    "totalling 3,456,000 bytes; layered-320x240 mode = "
+                    "14 native frames totalling 2,150,400 bytes. Returns ok / "
+                    "checksum / "
                     "bytes_transferred / error."
                 ),
                 inputSchema={
@@ -2787,10 +2789,11 @@ def create_server(notify_config: NotifyConfig | None = None) -> StackChanServer:
                         },
                         "mode": {
                             "type": "string",
-                            "enum": ["layered", "matrix"],
+                            "enum": ["layered", "matrix", "layered-320x240"],
                             "description": (
                                 "'layered' (14 frames, ~525 KB) or "
-                                "'matrix' (90 frames, ~3.3 MB)."
+                                "'matrix' (90 frames, ~3.3 MB), or "
+                                "'layered-320x240' (14 frames, ~2.1 MB)."
                             ),
                         },
                         "timeout": {
diff --git a/gateway/tests/test_capture_server.py b/gateway/tests/test_capture_server.py
index 91fecb3..6fb182f 100644
--- a/gateway/tests/test_capture_server.py
+++ b/gateway/tests/test_capture_server.py
@@ -268,3 +268,269 @@ async def test_capture_tolerates_non_utf8_question(tmp_path, monkeypatch):
             )
             resp = await client.post("/capture", data=mpwriter)
         assert resp.status == 200, await resp.text()
+
+
+def _framed_opus(*packets: bytes) -> bytes:
+    return b"".join(len(packet).to_bytes(2, "big") + packet for packet in packets)
+
+
+_OPUS_PACKET = bytes.fromhex(
+    "5802f9304dbb0de5e392098938ebcae1b1d1dd85"
+)
+
+
+def test_prepared_opus_parser_accepts_exact_framed_packets():
+    from stackchan_mcp.capture_server import _parse_prepared_opus
+
+    payload = _framed_opus(_OPUS_PACKET, _OPUS_PACKET)
+
+    assert _parse_prepared_opus(payload) == [_OPUS_PACKET, _OPUS_PACKET]
+
+
+@pytest.mark.parametrize(
+    ("packet", "message"),
+    (
+        (b"\x08x", "exactly 60 ms"),
+        (bytes((_OPUS_PACKET[0] | 0x04,)) + _OPUS_PACKET[1:], "mono"),
+        (b"\x5b", "frame count"),
+    ),
+)
+def test_prepared_opus_parser_rejects_wrong_profile(packet, message):
+    from stackchan_mcp.capture_server import (
+        PreparedOpusValidationError,
+        _parse_prepared_opus,
+    )
+
+    with pytest.raises(PreparedOpusValidationError, match=message):
+        _parse_prepared_opus(_framed_opus(packet))
+
+
+@pytest.mark.parametrize(
+    ("payload", "message"),
+    (
+        (b"", "empty"),
+        (b"\x00", "trailing bytes"),
+        (b"\x00\x00", "must not be empty"),
+        (b"\x00\x02x", "ends inside a packet"),
+        (
+            (1276).to_bytes(2, "big") + b"x" * 1276,
+            "packet exceeds",
+        ),
+    ),
+)
+def test_prepared_opus_parser_rejects_malformed_framing(payload, message):
+    from stackchan_mcp.capture_server import (
+        PreparedOpusValidationError,
+        _parse_prepared_opus,
+    )
+
+    with pytest.raises(PreparedOpusValidationError, match=message):
+        _parse_prepared_opus(payload)
+
+
+def test_prepared_opus_parser_enforces_total_and_packet_count_limits():
+    from stackchan_mcp.capture_server import (
+        PREPARED_OPUS_MAX_PACKETS,
+        PREPARED_OPUS_MAX_TOTAL_BYTES,
+        PreparedOpusTooLargeError,
+        PreparedOpusValidationError,
+        _parse_prepared_opus,
+    )
+
+    with pytest.raises(PreparedOpusTooLargeError):
+        _parse_prepared_opus(b"x" * (PREPARED_OPUS_MAX_TOTAL_BYTES + 1))
+    too_many_packets = _framed_opus(_OPUS_PACKET) * (
+        PREPARED_OPUS_MAX_PACKETS + 1
+    )
+    with pytest.raises(PreparedOpusValidationError, match="packets"):
+        _parse_prepared_opus(too_many_packets)
+
+
+def test_prepared_opus_route_requires_configured_audio_token():
+    from stackchan_mcp.capture_server import _is_authorized
+
+    app = create_capture_app(pcm_token="secret", gateway=object())
+    routes = {route.resource.canonical for route in app.router.routes()}
+
+    assert "/opus" in routes
+    assert _is_authorized("Bearer secret", "secret") is True
+    assert _is_authorized("Bearer wrong", "secret") is False
+    assert _is_authorized("", "") is False
+
+
+@pytest.mark.parametrize(
+    "message_id",
+    ("", ".bad", "bad id", "x" * 129),
+)
+def test_prepared_opus_message_id_requires_contract_shape(message_id):
+    from stackchan_mcp.capture_server import (
+        PreparedOpusValidationError,
+        _validate_prepared_opus_message_id,
+    )
+
+    with pytest.raises(PreparedOpusValidationError, match="X-Message-Id"):
+        _validate_prepared_opus_message_id(message_id)
+
+
+@pytest.mark.asyncio
+async def test_prepared_opus_retry_is_suppressed_while_id_retained(
+    monkeypatch,
+):
+    from aiohttp.test_utils import TestClient, TestServer
+    import stackchan_mcp.capture_server as capture_server
+
+    plays = []
+
+    async def record_play(gateway, packets):
+        del gateway
+        plays.append(packets)
+
+    monkeypatch.setattr(capture_server, "_play_prepared_opus", record_play)
+    app = create_capture_app(pcm_token="secret", gateway=object())
+    headers = {
+        "Authorization": "Bearer secret",
+        "Content-Type": "application/octet-stream",
+        "X-Message-Id": "eval:lost-response",
+    }
+
+    async with TestClient(TestServer(app)) as client:
+        first = await client.post(
+            "/opus",
+            data=_framed_opus(_OPUS_PACKET),
+            headers=headers,
+        )
+        assert first.status == 200
+        first.release()  # Model losing the completed response before reading it.
+        retry = await client.post(
+            "/opus",
+            data=_framed_opus(_OPUS_PACKET),
+            headers=headers,
+        )
+        assert retry.status == 200
+        assert (await retry.json())["ok"] is True
+
+    assert plays == [[_OPUS_PACKET]]
+
+
+@pytest.mark.asyncio
+async def test_prepared_opus_concurrent_duplicate_is_coalesced(monkeypatch):
+    import asyncio
+    from aiohttp.test_utils import TestClient, TestServer
+    import stackchan_mcp.capture_server as capture_server
+
+    plays = []
+
+    async def record_play(gateway, packets):
+        del gateway
+        plays.append(packets)
+        await asyncio.sleep(0)
+
+    monkeypatch.setattr(capture_server, "_play_prepared_opus", record_play)
+    app = create_capture_app(pcm_token="secret", gateway=object())
+    headers = {
+        "Authorization": "Bearer secret",
+        "Content-Type": "application/octet-stream",
+        "X-Message-Id": "eval:concurrent",
+    }
+
+    async with TestClient(TestServer(app)) as client:
+        responses = await asyncio.gather(
+            client.post(
+                "/opus", data=_framed_opus(_OPUS_PACKET), headers=headers
+            ),
+            client.post(
+                "/opus", data=_framed_opus(_OPUS_PACKET), headers=headers
+            ),
+        )
+        assert [response.status for response in responses] == [200, 200]
+
+    assert plays == [[_OPUS_PACKET]]
+
+
+@pytest.mark.asyncio
+async def test_prepared_opus_eviction_allows_replay(monkeypatch):
+    from aiohttp.test_utils import TestClient, TestServer
+    import stackchan_mcp.capture_server as capture_server
+
+    plays = []
+
+    async def record_play(gateway, packets):
+        del gateway
+        plays.append(packets)
+
+    monkeypatch.setattr(capture_server, "_play_prepared_opus", record_play)
+    monkeypatch.setattr(capture_server, "PREPARED_OPUS_MAX_RECORDED_IDS", 2)
+    app = create_capture_app(pcm_token="secret", gateway=object())
+    headers = {
+        "Authorization": "Bearer secret",
+        "Content-Type": "application/octet-stream",
+    }
+
+    async with TestClient(TestServer(app)) as client:
+        for index in range(3):
+            headers["X-Message-Id"] = f"eval:bounded:{index}"
+            response = await client.post(
+                "/opus",
+                data=_framed_opus(_OPUS_PACKET),
+                headers=headers,
+            )
+            assert response.status == 200
+        headers["X-Message-Id"] = "eval:bounded:0"
+        replay = await client.post(
+            "/opus",
+            data=_framed_opus(_OPUS_PACKET),
+            headers=headers,
+        )
+        assert replay.status == 200
+
+    assert list(app[capture_server.PREPARED_OPUS_RESULTS_KEY]) == [
+        "eval:bounded:2",
+        "eval:bounded:0",
+    ]
+    assert plays == [[_OPUS_PACKET]] * 4
+
+
+@pytest.mark.asyncio
+async def test_prepared_opus_playback_is_locked_bracketed_and_paced(
+    monkeypatch,
+):
+    import asyncio
+    import stackchan_mcp.capture_server as capture_server
+
+    events = []
+    sleeps = []
+
+    class FakeESP32:
+        def __init__(self):
+            self.tts_lock = asyncio.Lock()
+
+        async def send_tts_state(self, state):
+            assert self.tts_lock.locked()
+            events.append(("tts", state))
+
+        async def send_audio_frame(self, packet):
+            assert self.tts_lock.locked()
+            events.append(("frame", packet))
+
+    class FakeGateway:
+        def __init__(self):
+            self.esp32 = FakeESP32()
+
+    async def record_sleep(delay):
+        sleeps.append(delay)
+
+    monkeypatch.setattr(capture_server.asyncio, "sleep", record_sleep)
+
+    packets = [f"packet-{index}".encode() for index in range(8)]
+    await capture_server._play_prepared_opus(
+        FakeGateway(),
+        packets,
+    )
+
+    assert events == [
+        ("tts", "start"),
+        *[("frame", packet) for packet in packets],
+        ("tts", "stop"),
+    ]
+    assert sleeps[0] == capture_server.PREPARED_OPUS_START_DELAY_S
+    assert len(sleeps) > 1
diff --git a/gateway/tests/test_gateway.py b/gateway/tests/test_gateway.py
index 2f849c7..956accd 100644
--- a/gateway/tests/test_gateway.py
+++ b/gateway/tests/test_gateway.py
@@ -82,6 +82,72 @@ def test_get_gateway_singleton():
     gw_mod._gateway = None


+@pytest.mark.asyncio
+@pytest.mark.parametrize(
+    ("mode", "size"),
+    (
+        ("layered", 14 * 160 * 120 * 2),
+        ("matrix", 90 * 160 * 120 * 2),
+        ("layered-320x240", 14 * 320 * 240 * 2),
+    ),
+)
+async def test_load_avatar_set_accepts_supported_payload_sizes(
+    monkeypatch, tmp_path, mode, size
+):
+    import stackchan_mcp.gateway as gw_mod
+
+    payload = tmp_path / "avatar.rgb565le"
+    payload.write_bytes(b"x" * size)
+    gateway = Gateway()
+    gateway._capture_app = object()
+    monkeypatch.setattr(
+        gw_mod,
+        "stage_avatar_set",
+        lambda app, staged_mode, data: _stage_result(
+            app, staged_mode, data, mode, size
+        ),
+    )
+
+    class FakeEsp32:
+        async def send_avatar_set_fetch(self, **kwargs):
+            assert kwargs["mode"] == mode
+            assert kwargs["expected_size"] == size
+            return {"ok": True, "checksum": kwargs["checksum"]}
+
+    gateway.esp32 = FakeEsp32()
+    result = await gateway.load_avatar_set(str(payload), mode)
+
+    assert result["ok"] is True
+    assert result["bytes_transferred"] == size
+
+
+async def _stage_result(app, staged_mode, data, expected_mode, expected_size):
+    assert app is not None
+    assert staged_mode == expected_mode
+    assert len(data) == expected_size
+    return "short-id", "token", "sha256:test"
+
+
+@pytest.mark.asyncio
+async def test_load_avatar_set_rejects_wrong_native_payload_size(tmp_path):
+    payload = tmp_path / "avatar.rgb565le"
+    payload.write_bytes(b"x" * (14 * 160 * 120 * 2))
+    gateway = Gateway()
+    gateway._capture_app = object()
+
+    result = await gateway.load_avatar_set(
+        str(payload), "layered-320x240"
+    )
+
+    assert result == {
+        "ok": False,
+        "error": (
+            "size_mismatch: got=537600 expected=2150400 "
+            "(mode=layered-320x240)"
+        ),
+    }
+
+
 def test_vision_url_uses_explicit_url(monkeypatch):
     """VISION_URL overrides host/port construction for remote tunnels."""
     monkeypatch.setenv("VISION_URL", "https://stackchan.example.ts.net:8443/capture")
diff --git a/firmware/main/CMakeLists.txt b/firmware/main/CMakeLists.txt
index 01591f0..01c3bd7 100644
--- a/firmware/main/CMakeLists.txt
+++ b/firmware/main/CMakeLists.txt
@@ -1042,2 +1042,3 @@ idf_component_register(SRCS ${SOURCES}
                         esp_driver_uart
+                        esp_driver_usb_serial_jtag
                         esp_driver_spi
diff --git a/firmware/main/boards/common/power_save_timer.cc b/firmware/main/boards/common/power_save_timer.cc
index 522e64c..b0c3c42 100644
--- a/firmware/main/boards/common/power_save_timer.cc
+++ b/firmware/main/boards/common/power_save_timer.cc
@@ -5,3 +5,6 @@
 #include <esp_log.h>
-
+#if defined(CONFIG_BOARD_TYPE_STACKCHAN) && \
+    defined(CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG)
+#include <driver/usb_serial_jtag.h>
+#endif
 #define TAG "PowerSaveTimer"
@@ -62,2 +65,12 @@ void PowerSaveTimer::OnShutdownRequest(std::function<void()> callback) {
 void PowerSaveTimer::PowerSaveCheck() {
+#if defined(CONFIG_BOARD_TYPE_STACKCHAN) && \
+    defined(CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG)
+    if (usb_serial_jtag_is_connected()) {
+        ticks_ = 0;
+        if (in_sleep_mode_) {
+            WakeUp();
+        }
+        return;
+    }
+#endif
     auto& app = Application::GetInstance();
diff --git a/firmware/main/boards/stackchan/config.json b/firmware/main/boards/stackchan/config.json
index 2cfccf7..ae43d6a 100644
--- a/firmware/main/boards/stackchan/config.json
+++ b/firmware/main/boards/stackchan/config.json
@@ -10,2 +10,3 @@
                 "CONFIG_CAMERA_GC0308_DVP_YUV422_320X240_20FPS=y",
+                "CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y",
                 "CONFIG_STACKCHAN_SERVO_FEETECH=y"
@@ -14,2 +15,2 @@
     ]
-}
\ No newline at end of file
+}
diff --git a/firmware/main/boards/stackchan/stackchan.cc b/firmware/main/boards/stackchan/stackchan.cc
index 83f77b4..6dfc306 100644
--- a/firmware/main/boards/stackchan/stackchan.cc
+++ b/firmware/main/boards/stackchan/stackchan.cc
@@ -33,2 +33,3 @@ static inline bool ServoWritePosOk(int r) { return r > 0; }
 #include "avatar_set.h"
+#include "usb_control.h"
 #include "avatar_set_fetcher.h"
@@ -7098,2 +7099,3 @@ public:
         RegisterMcpTools();
+        StartStackChanUsbControl();
     }
diff --git a/firmware/main/boards/stackchan/usb_control.cc b/firmware/main/boards/stackchan/usb_control.cc
new file mode 100644
index 0000000..675c8d2
--- /dev/null
+++ b/firmware/main/boards/stackchan/usb_control.cc
@@ -0,0 +1,274 @@
+#include "usb_control.h"
+
+#include <cstdio>
+#include <cstring>
+#include <string>
+
+#include <cJSON.h>
+#include <esp_app_desc.h>
+#include <esp_log.h>
+#include <freertos/FreeRTOS.h>
+#include <freertos/task.h>
+#include <wifi_manager.h>
+
+#include "application.h"
+#include "device_state_machine.h"
+#include "settings.h"
+
+namespace {
+
+constexpr char kRequestPrefix[] = "XC_BODY_REQUEST ";
+constexpr char kResponsePrefix[] = "XC_BODY_RESPONSE ";
+constexpr size_t kMaxRequestBytes = 1024;
+constexpr uint32_t kTaskStackSize = 6144;
+constexpr UBaseType_t kTaskPriority = tskIDLE_PRIORITY + 1;
+const char* const kTag = "StackChanUsb";
+
+bool IsString(const cJSON* value) {
+    return value != nullptr && cJSON_IsString(value) &&
+        value->valuestring != nullptr;
+}
+
+bool HasGatewayScheme(const std::string& value) {
+    return value.empty() || value.rfind("ws://", 0) == 0 ||
+        value.rfind("wss://", 0) == 0;
+}
+
+bool IsUrlForced() {
+#if defined(CONFIG_FORCE_DEFAULT_WEBSOCKET_URL) && \
+    defined(CONFIG_DEFAULT_WEBSOCKET_URL)
+    return CONFIG_DEFAULT_WEBSOCKET_URL[0] != '\0';
+#else
+    return false;
+#endif
+}
+
+bool IsFallbackUrlForced() {
+#if defined(CONFIG_FORCE_DEFAULT_WEBSOCKET_URL) && \
+    defined(CONFIG_DEFAULT_WEBSOCKET_FALLBACK_URL)
+    return CONFIG_DEFAULT_WEBSOCKET_FALLBACK_URL[0] != '\0';
+#else
+    return false;
+#endif
+}
+
+bool IsTokenForced() {
+#if defined(CONFIG_FORCE_DEFAULT_WEBSOCKET_URL) && \
+    defined(CONFIG_DEFAULT_WEBSOCKET_TOKEN)
+    return CONFIG_DEFAULT_WEBSOCKET_TOKEN[0] != '\0';
+#else
+    return false;
+#endif
+}
+
+cJSON* NewResponse(const char* command, bool ok) {
+    auto response = cJSON_CreateObject();
+    cJSON_AddStringToObject(response, "command", command);
+    cJSON_AddBoolToObject(response, "ok", ok);
+    return response;
+}
+
+void SendResponse(cJSON* response) {
+    char* json = cJSON_PrintUnformatted(response);
+    if (json != nullptr) {
+        std::printf("%s%s\n", kResponsePrefix, json);
+        std::fflush(stdout);
+        cJSON_free(json);
+    }
+    cJSON_Delete(response);
+}
+
+void SendError(const char* command, const char* error) {
+    auto response = NewResponse(command, false);
+    cJSON_AddStringToObject(response, "error", error);
+    SendResponse(response);
+}
+
+void AddGatewayStatus(cJSON* response) {
+    Settings settings("websocket", false);
+    cJSON_AddStringToObject(
+        response, "url", settings.GetString("url").c_str());
+    cJSON_AddStringToObject(
+        response,
+        "fallback_url",
+        settings.GetString("fallback_url").c_str());
+    cJSON_AddBoolToObject(
+        response,
+        "token_set",
+        !settings.GetString("token").empty());
+    cJSON_AddBoolToObject(response, "url_forced", IsUrlForced());
+    cJSON_AddBoolToObject(
+        response, "fallback_url_forced", IsFallbackUrlForced());
+    cJSON_AddBoolToObject(response, "token_forced", IsTokenForced());
+}
+
+void SendStatus() {
+    auto& app = Application::GetInstance();
+    auto& wifi = WifiManager::GetInstance();
+    auto response = NewResponse("status", true);
+    cJSON_AddStringToObject(
+        response,
+        "firmware",
+        esp_app_get_description()->version);
+    cJSON_AddStringToObject(
+        response,
+        "state",
+        DeviceStateMachine::GetStateName(app.GetDeviceState()));
+    cJSON_AddBoolToObject(response, "wifi_connected", wifi.IsConnected());
+    cJSON_AddStringToObject(response, "ssid", wifi.GetSsid().c_str());
+    cJSON_AddStringToObject(response, "ip", wifi.GetIpAddress().c_str());
+    cJSON_AddNumberToObject(response, "rssi", wifi.GetRssi());
+    AddGatewayStatus(response);
+    SendResponse(response);
+}
+
+bool GetOptionalString(
+    const cJSON* request,
+    const char* key,
+    bool& provided,
+    std::string& value) {
+    const cJSON* item = cJSON_GetObjectItemCaseSensitive(request, key);
+    provided = item != nullptr;
+    if (!provided) {
+        return true;
+    }
+    if (!IsString(item)) {
+        return false;
+    }
+    value = item->valuestring;
+    return true;
+}
+
+void SaveGatewayConfig(const cJSON* request) {
+    bool url_provided = false;
+    bool fallback_provided = false;
+    bool token_provided = false;
+    std::string url;
+    std::string fallback_url;
+    std::string token;
+    if (!GetOptionalString(
+            request, "url", url_provided, url) ||
+        !GetOptionalString(
+            request,
+            "fallback_url",
+            fallback_provided,
+            fallback_url) ||
+        !GetOptionalString(
+            request, "token", token_provided, token)) {
+        SendError("configure", "configuration values must be strings");
+        return;
+    }
+    if (!url_provided && !fallback_provided && !token_provided) {
+        SendError("configure", "no configuration value was provided");
+        return;
+    }
+    if ((url_provided && !HasGatewayScheme(url)) ||
+        (fallback_provided && !HasGatewayScheme(fallback_url))) {
+        SendError("configure", "gateway URLs must use ws:// or wss://");
+        return;
+    }
+    if ((url_provided && IsUrlForced()) ||
+        (fallback_provided && IsFallbackUrlForced()) ||
+        (token_provided && IsTokenForced())) {
+        SendError(
+            "configure",
+            "requested value is overridden by this firmware build");
+        return;
+    }
+
+    {
+        Settings settings("websocket", true);
+        auto save = [&settings](
+                        const char* key,
+                        bool provided,
+                        const std::string& value) {
+            if (!provided) {
+                return;
+            }
+            if (value.empty()) {
+                settings.EraseKey(key);
+            } else {
+                settings.SetString(key, value);
+            }
+        };
+        save("url", url_provided, url);
+        save("fallback_url", fallback_provided, fallback_url);
+        save("token", token_provided, token);
+    }
+
+    auto response = NewResponse("configure", true);
+    cJSON_AddStringToObject(response, "takes_effect", "after_reboot");
+    AddGatewayStatus(response);
+    SendResponse(response);
+}
+
+void ScheduleReboot() {
+    SendResponse(NewResponse("reboot", true));
+    Application::GetInstance().Schedule([]() {
+        Application::GetInstance().Reboot();
+    });
+}
+
+void HandleRequest(const char* json) {
+    cJSON* request = cJSON_Parse(json);
+    if (request == nullptr || !cJSON_IsObject(request)) {
+        cJSON_Delete(request);
+        SendError("unknown", "invalid JSON request");
+        return;
+    }
+    const cJSON* command =
+        cJSON_GetObjectItemCaseSensitive(request, "command");
+    if (!IsString(command)) {
+        cJSON_Delete(request);
+        SendError("unknown", "command must be a string");
+        return;
+    }
+
+    if (std::strcmp(command->valuestring, "status") == 0) {
+        SendStatus();
+    } else if (std::strcmp(command->valuestring, "configure") == 0) {
+        SaveGatewayConfig(request);
+    } else if (std::strcmp(command->valuestring, "reboot") == 0) {
+        ScheduleReboot();
+    } else {
+        SendError(command->valuestring, "unsupported command");
+    }
+    cJSON_Delete(request);
+}
+
+void UsbControlTask(void*) {
+    char line[kMaxRequestBytes];
+    ESP_LOGI(kTag, "USB maintenance control ready");
+    while (true) {
+        if (std::fgets(line, sizeof(line), stdin) == nullptr) {
+            clearerr(stdin);
+            vTaskDelay(pdMS_TO_TICKS(20));
+            continue;
+        }
+        size_t length = std::strlen(line);
+        while (length > 0 &&
+               (line[length - 1] == '\n' || line[length - 1] == '\r')) {
+            line[--length] = '\0';
+        }
+        if (std::strncmp(line, kRequestPrefix, sizeof(kRequestPrefix) - 1) !=
+            0) {
+            continue;
+        }
+        HandleRequest(line + sizeof(kRequestPrefix) - 1);
+    }
+}
+
+}  // namespace
+
+void StartStackChanUsbControl() {
+    BaseType_t created = xTaskCreate(
+        UsbControlTask,
+        "stackchan_usb",
+        kTaskStackSize,
+        nullptr,
+        kTaskPriority,
+        nullptr);
+    if (created != pdPASS) {
+        ESP_LOGE(kTag, "Failed to start USB maintenance control");
+    }
+}
diff --git a/firmware/main/boards/stackchan/usb_control.h b/firmware/main/boards/stackchan/usb_control.h
new file mode 100644
index 0000000..7a605d9
--- /dev/null
+++ b/firmware/main/boards/stackchan/usb_control.h
@@ -0,0 +1,6 @@
+#ifndef STACKCHAN_USB_CONTROL_H_
+#define STACKCHAN_USB_CONTROL_H_
+
+void StartStackChanUsbControl();
+
+#endif  // STACKCHAN_USB_CONTROL_H_
